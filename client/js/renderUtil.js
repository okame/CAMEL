/**
 * renderUtil.js
 *	called "display.init" when client get ready message from server with stage ENV.
 *	called "display.render" method at "evLoop" on game.js per frame rate.
 *
 * dep:
 *
 */

renderUtil = {};

(function($) {
 renderUtil.getPackPos
 })($);
